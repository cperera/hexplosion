require IEx;
import Game;
import Map

defmodule Drink do
    @doc """
    In order to drink, a player interacts with a liquid.
    ## Example
        iex> Drink(player, liquid)
        player, liquid
    """
    # consume liquid if consumable
    # apply effects
    def apply_effect(player, liquid, amount) do
        nil
    end
end

